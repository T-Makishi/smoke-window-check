# 正式版のログイン・パスコード運用

## 業者設定・運営者ログイン

1. 管理URLを開きます。
2. 登録メールアドレスへCloudflareの確認コードを送信します。
3. 確認コードを入力します。
4. 初回だけ、6〜8桁の数字で専用パスコードを登録します。
5. 2回目以降は、メール確認後に登録済みパスコードを入力します。

メール確認コードは1回限り有効で、発行後10分で失効します。新しいコードを再送すると、それ以前のコードは無効になります。届かない場合は迷惑メールフォルダを確認し、`noreply@notify.cloudflare.com` を受信可能にしてください。

## パスコードを忘れた場合

1. ログイン画面の「パスコードを忘れた場合」を押します。
2. 「URLをコピーしてログアウト」を押します。
3. コピーした再設定用URLを開きます。
4. 登録メールアドレスへ新しい確認コードを送信して認証します。
5. 新しいパスコードを登録します。

独自の再設定メールは送信しません。Cloudflareのメール確認を本人確認として使用します。

## セキュリティ仕様

- パスコードは登録メールアドレスごとに設定します。
- 平文では保存せず、ランダムソルト付きPBKDF2-SHA-256ハッシュとしてD1へ保存します。
- 5回連続で失敗すると15分間ロックします。
- アプリのパスコード認証は24時間有効です。
- 登録メールアドレスを変更すると、旧パスコードと旧セッションは失効します。
- 固定パスコード「1234」は正式なCloudflare URLでは使用しません。

## 利用区分

- 試験利用：7日、14日、30日から選択します。
- 本番利用：期限なしで利用できます。
- 運営者は、試験利用から本番利用への切替、試験期間の再発行、利用停止、利用再開を操作できます。
