# 7日間無料体験・本番利用申込 設計書

## 1. 運用方針

- 公開URLは `smoke-window-check.pages.dev` を継続使用し、独自ドメインは使用しない。
- 新規業者は公開申込ページから7日間の無料体験を申請する。
- メールアドレスの所有確認が完了した時点で、業者識別番号と専用URLを自動発行する。
- 従来の運営者による手動登録も残し、紹介先や個別対応へ使用する。
- 本番利用は自動契約にせず、業者からの申請を運営者が確認して切り替える。
- 顧客の問診、写真、動画は従来どおり顧客端末内だけで扱う。

## 2. 画面とURL

| 対象 | URL | 用途 |
| --- | --- | --- |
| 新規業者 | `/trial.html` | サービス説明、無料体験申込、メール確認結果 |
| 顧客 | `/?t=<tenant_id>` | 問診入力。ログイン不要 |
| 業者 | `/?t=<tenant_id>&admin=1` | 自社設定、本番利用申請 |
| 運営者 | `/service.html` | 申込一覧、停止、再開、本番切替 |
| 定期処理 | `/api/jobs/reminders` | 期限3日前・1日前の案内メール |

## 3. 申込処理

1. 会社名、担当者名、メール、電話番号を入力し、利用条件へ同意する。
2. サーバーが入力、重複申込、送信頻度、bot用隠し項目を検査する。
3. 30分有効の確認リンクをメール送信する。
4. 申込者が確認リンクを開く。
5. D1へ7日間の業者登録を作成し、推測困難な業者識別番号を発行する。
6. 申込者へ業者設定URL・顧客案内URLを自動返信し、運営者へ申込通知を送る。
7. 運営者画面へ申込者を表示する。

## 4. 不正利用・誤操作対策

- 同一メールアドレスの無料体験は1回に制限する。
- 同一送信元からの申込回数を24時間単位で制限する。
- 確認トークンは平文保存せずSHA-256ハッシュだけをD1へ保存する。
- 確認リンクは30分で無効化し、使用後は再利用できない。
- 申込画面から本番利用へ直接切り替えず、運営者承認を必須にする。
- APIキー、ジョブ認証キーはCloudflareの暗号化シークレットへ保存する。

## 5. メール

- 送信APIはBrevoのトランザクションメールAPIを使用する。
- 試験運用では検証済みGmailアドレスを送信者として登録できる構成にする。
- `BREVO_API_KEY`、`EMAIL_SENDER_ADDRESS`、`EMAIL_SENDER_NAME` をCloudflareへ登録する。
- 送信成否と用途を `email_events` に記録し、期限案内の二重送信を防ぐ。
- 本格販売へ移行する時点で独自ドメインを取得し、SPF・DKIM・DMARCを設定する。

## 6. 本番利用申込

- 業者設定画面から本番利用の相談申込を送信する。
- 申込時点では課金または契約成立としない。
- 運営者が料金・契約条件を個別確認し、運営画面で本番利用へ切り替える。
- オンライン決済・電子契約は利用件数と成約数を確認後の次期機能とする。
